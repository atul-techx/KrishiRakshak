{
  "name": "krishirakshak-portable",
  "version": "8.0.0",
  "private": true,
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.js tests/*.test.mjs",
    "check": "node check-config.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
